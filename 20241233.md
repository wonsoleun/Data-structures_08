# Data-structures_08

1. Chaining 장점: 충돌이 발생해도 그냥 리스트 끝에 붙이면 되므로 복잡한 탐사 알고리즘이 필요 없다. 연결 리스트에서 노드를 삭제하는 것만큼 간단하다. <br>
  단점: 데이터들이 메모리 곳곳에 흩어져 저장될 확률이 높아, CPU 캐시 히트율(Cache Hit Rate)이 떨어질 수 있다. <br>
Open ADD 장점: 추가적인 연결 리스트나 포인터가 필요 없어, 테이블 내부 공간을 효율적으로 사용한다. <br>
  단점: 테이블이 꽉 차갈수록 빈 자리를 찾는 시간이 기하급수적으로 늘어난다. 보통 전체 용량의 70%~80%가 차면 테이블 크기를 늘리고 재해싱(Rehashing)을 해야 한다. <br>

2-1. 해시 함수때문에, 결과값은 항상 0에서 1023사이의 정수만 나온다. 하지만 테이블 크기는 1,204로 잡았기 때문에, 인덱스 1024부터 1203까지의 공간(180개 슬롯)은 절대로 사용되지 않는다.
  즉, 전체 메모리의 180개의 슬롯을 버리는 문제가 발생한다.

2-2. 데이터 검색이 불가능하기 때문에 사용할 수 없다. 데이터를 저장할 때 1번 인덱스에 넣었다고 가정하자. 나중에 이 데이터를 찾으려고 다시 호출하면, 내부의 random 함수 때문에 이번에는 5번 인덱스가 나올 수 있다.
  결국 저장한 위치를 다시 찾을 수 없게 되므로, 데이터를 관리하는 용도로는 전혀 사용할 수 없다.

2-4. 해시 테이블은 다음과 같다. [ - , 40 , - , - , 30 , - , - , 20 , 33 , 46 , 10 , 50 , 60 ] <br>
  왜냐하면 차례대로 삽입하면 33, 46, 60에서 충돌이 발생하고 33을 8번 인덱스에, 46을 9번 인덱스에, 60을 12번 인덱스에 저장하게 되기 때문이다.

2-5. 이차원 탐색을 할 경우의 해시 테이블은 다음과 같다. [ - , 40 , - , - , 30 , - , - , 20 , 33 , 60 , 10 , 46 , 50 ] <br>
  왜냐하면 33은 8번 인덱스에, 46은 11번 인덱스에, 50은 12번 인덱스에, 60은 9번 인덱스에 저장되기 때문이다.

2-6. 더블 해싱을 한 경우의 해시 테이블은 다음과 같다. [ 46 , 40 , - , - , 30 , - , 60 , 20 , 33 , - , 10 , 50 , - ] <br>
  왜냐하면 더블 해싱이기에 60에서 4번의 충돌이 일어나고 충돌이 일어날 때마다 더블해싱 함수를 사용하면 60은 6번 인덱스에 저장되기 때문이다. 

2-7. 일반적인 chainng에서는 삽입 시 O(1)의 성능을 내기 위해 보통 리스트의 Head에 새 키를 넣는다. 만약 이를 Tail에 삽입하도록 바꾼다면 맨 뒤에 삽입하기 위해 리스트의 처음부터 끝까지 모든 노드를 탐색해야 한다. 
  이 경우 삽입 효율이 연결 리스트의 길이인 n에 비례하게 되고 성능은 O(n)이 된다.

2-8. 삽입: 정렬되지 않은 리스트는 맨 앞에 넣으면 끝이지만, 정렬된 리스트는 자기 자리를 찾기 위해 리스트를 훑어야 하므로 평균 n/2번의 비교가 발생한다. <br>
  삭제: 두 방식 모두 삭제할 노드를 먼저 찾아야 하므로 탐색 효율에 의존하게 된다. <br>
  성공적인 검색: 찾으려는 키가 리스트에 있다면, 정렬 여부와 상관없이 평균적으로 리스트 길이의 절반 정도를 확인하게 된다. <br>
  실패하는 검색: 만약 정렬이 안돼 있다면, 리스트 끝까지 가봐야만 없음을 확신할 수 있다. 만약 정렬 되어있다면, 검색 도중 내가 찾는 값보다 더 큰 값이 나오면 그 뒤는 더 볼 필요가 없고, 따라서 리스트를 끝까지 다 보지 않아도 되므로 평균 비교 횟수가 약 절반으로 줄어든다.
